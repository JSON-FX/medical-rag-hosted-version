# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets — never commit. .env.example carries key names only.
.env
.env.*
!.env.example

# macOS
.DS_Store

# Per-developer Claude Code settings. The shared AI layer (plans, reports,
# skills) is committed; machine-local overrides are not.
.claude/settings.local.json
